document.addEventListener("DOMContentLoaded",async()=>{const data=await BERLContent.rows('projects');document.getElementById("project-list").innerHTML=data.map(p=>`<article class="card project-card" id="${BERL.esc(p.slug||'')}"><div class="project-image" style="background-image:url('${BERL.esc(p.image||"assets/images/hero-reactor.svg")}')"></div><div class="project-copy"><span class="pill">${BERL.esc(p.status||'')}</span><h3>${BERL.esc(p.title)}</h3><p>${BERL.esc(p.summary||'')}</p><div class="detail-list"><div class="detail-row"><strong>Period</strong><span>${BERL.esc(p.period||'')}</span></div><div class="detail-row"><strong>Research area</strong><span>${BERL.esc(p.area||'')}</span></div><div class="detail-row"><strong>Lead</strong><span>${BERL.esc(p.lead||'')}</span></div></div><div class="project-meta">${(p.keywords||[]).map(k=>`<span class="tag">${BERL.esc(k)}</span>`).join("")}</div></div></article>`).join("")});