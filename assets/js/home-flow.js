(()=>{const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduced)return;let ticking=false;const clamp=(n,a,b)=>Math.max(a,Math.min(b,n));const update=()=>{const y=window.scrollY||0;document.documentElement.style.setProperty('--hero-y',`${clamp(y*.17,0,115)}px`);const scene=document.getElementById('motion-scene');if(scene){const r=scene.getBoundingClientRect();const p=clamp((innerHeight-r.top)/(innerHeight+r.height),0,1);scene.style.setProperty('--scene-bg-y',`${(p-.5)*105}px`);scene.style.setProperty('--scene-copy-y',`${(0.5-p)*38}px`);scene.style.setProperty('--scene-art-y',`${(p-.5)*54}px`);scene.style.setProperty('--scene-scale',`${0.955+p*.07}`)}ticking=false};const request=()=>{if(ticking)return;ticking=true;requestAnimationFrame(update)};addEventListener('scroll',request,{passive:true});addEventListener('resize',request,{passive:true});request()})();
