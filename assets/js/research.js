document.addEventListener('DOMContentLoaded',async()=>{const data=await BERLContent.rows('research');document.getElementById('research-list').innerHTML=data.map(r=>`<article class="card project-card" id="${BERL.esc(r.slug)}"><div class="project-image" style="background-image:url('${BERL.esc(r.image||'assets/images/generated/hero-clean-tech.png')}')"></div><div class="project-copy"><div class="card-code">${BERL.esc(r.code||'')}</div><span class="pill">${BERL.esc(r.tag||'')}</span><h3>${BERL.esc(r.title)}</h3><p>${BERL.esc(r.summary||'')}</p><div class="project-meta">${(r.topics||[]).map(t=>`<span class="tag">${BERL.esc(t)}</span>`).join('')}</div></div></article>`).join('')});