document.addEventListener('DOMContentLoaded',async()=>{try{const[cfg,metrics,pubs,research,news]=await Promise.all([BERL.json('data/site.json'),BERL.json('data/metrics.json'),BERL.json('data/publications.json'),BERL.json('data/research.json'),BERL.json('data/news.json')]);const detail=n=>`news-detail.html?id=${encodeURIComponent(n.slug||'')}`;document.getElementById('hero-kicker').textContent=cfg.tagline||'Bioenergy · Remediation · Circular Systems';document.getElementById('hero-title').innerHTML=BERL.esc(cfg.hero_title||'Engineering cleaner systems for a circular future.').replace('circular future.','<em>circular future.</em>');document.getElementById('hero-desc').textContent=cfg.hero_description||'';document.getElementById('hero-stats').innerHTML=[['Bioenergy','Waste-to-resource'],['Remediation','Environmental materials'],['Biotechnology','Microbial systems'],['Data','AI-driven engineering']].map(([a,b])=>`<span class="hero-stat">${a} · ${b}</span>`).join('');
const heroPhotoIndexes=[1,0,2,3,4,5];document.getElementById('hero-slides').innerHTML=heroPhotoIndexes.map((idx,i)=>`<div class="hero-slide ${i===0?'active':''}" style="background-position:${BERL.spritePos(idx)}"></div>`).join('');document.getElementById('hero-dots').innerHTML=heroPhotoIndexes.map((_,i)=>`<button class="hero-dot ${i===0?'active':''}" data-index="${i}" aria-label="Background slide ${i+1}"></button>`).join('');document.querySelector('.hero-showcase').insertAdjacentHTML('beforeend',`<div class="hero-slide-nav"><button id="hero-prev" aria-label="Previous background">←</button><span id="hero-count">01 / 06</span><button id="hero-next" aria-label="Next background">→</button></div>`);let heroIndex=0;const heroSlides=[...document.querySelectorAll('.hero-slide')],heroDots=[...document.querySelectorAll('.hero-dot')];const setHero=i=>{heroIndex=(i+heroSlides.length)%heroSlides.length;heroSlides.forEach((el,j)=>el.classList.toggle('active',j===heroIndex));heroDots.forEach((el,j)=>el.classList.toggle('active',j===heroIndex));document.getElementById('hero-count').textContent=`${String(heroIndex+1).padStart(2,'0')} / ${String(heroSlides.length).padStart(2,'0')}`};let heroTimer;const resetHero=()=>{clearInterval(heroTimer);heroTimer=setInterval(()=>setHero(heroIndex+1),6400)};heroDots.forEach(d=>d.onclick=()=>{setHero(Number(d.dataset.index));resetHero()});document.getElementById('hero-prev').onclick=()=>{setHero(heroIndex-1);resetHero()};document.getElementById('hero-next').onclick=()=>{setHero(heroIndex+1);resetHero()};setHero(0);resetHero();
const latest=[...news].sort((a,b)=>String(b.date).localeCompare(String(a.date))).slice(0,5);document.getElementById('hero-news-list').innerHTML=latest.map((n,i)=>`<a class="hero-news-item ${i===0?'active':''}" data-index="${i}" href="${detail(n)}"><div class="date">${BERL.esc(n.date)} · ${BERL.esc(n.category||'News')}</div><strong>${BERL.esc(n.title)}</strong><p>${BERL.esc(n.summary||'')}</p></a>`).join('');let newsIndex=0;const newsEls=[...document.querySelectorAll('.hero-news-item')];const setNews=i=>{newsIndex=(i+newsEls.length)%newsEls.length;newsEls.forEach((el,j)=>el.classList.toggle('active',j===newsIndex));const p=document.querySelector('.news-progress span');if(p)p.style.transform=`translateX(${newsIndex*100}%)`};let newsTimer;const resetNews=()=>{clearInterval(newsTimer);newsTimer=setInterval(()=>setNews(newsIndex+1),3900)};newsEls.forEach((el,i)=>el.addEventListener('mouseenter',()=>{setNews(i);resetNews()}));setNews(0);resetNews();
if(!document.querySelector('.motion-marquee'))document.querySelector('.hero-showcase').insertAdjacentHTML('afterend','<div class="motion-marquee"><div class="motion-marquee-track"><span>Bioenergy</span><span>Resource Recovery</span><span>Environmental Remediation</span><span>Microbial Systems</span><span>Circular Materials</span><span>Organic Matter Transformation</span><span>Data-Driven Engineering</span><span>Bioenergy</span><span>Resource Recovery</span><span>Environmental Remediation</span><span>Microbial Systems</span><span>Circular Materials</span><span>Organic Matter Transformation</span><span>Data-Driven Engineering</span></div></div>');
document.getElementById('research-track').innerHTML=research.map((r,i)=>`<a class="carousel-card" href="research.html#${BERL.esc(r.slug)}"><div class="media sprite-photo" style="background-position:${BERL.spritePos(r.photo_index??i)}"></div><div class="overlay"><span class="pill">${BERL.esc(r.tag)}</span><h3>${BERL.esc(r.title)}</h3><p>${BERL.esc(r.summary)}</p><span class="small">${BERL.esc((r.topics||[]).slice(0,3).join(' · '))}</span></div></a>`).join('');const track=document.getElementById('research-track'),dots=document.getElementById('research-dots');let page=0,perView=3;const calc=()=>innerWidth<=720?1:(innerWidth<=1080?2:3),pages=()=>Math.max(1,Math.ceil(research.length/perView));function renderDots(){dots.innerHTML=Array.from({length:pages()},(_,i)=>`<button class="${i===page?'active':''}" data-index="${i}"></button>`).join('');[...dots.children].forEach(b=>b.onclick=()=>{page=Number(b.dataset.index);update();resetResearch()})}function update(){perView=calc();if(page>=pages())page=0;const first=track.querySelector('.carousel-card'),gap=18,w=first?first.getBoundingClientRect().width:0;track.style.transform=`translateX(-${(w+gap)*perView*page}px)`;[...dots.children].forEach((d,i)=>d.classList.toggle('active',i===page))}document.getElementById('research-prev').onclick=()=>{page=(page-1+pages())%pages();update();resetResearch()};document.getElementById('research-next').onclick=()=>{page=(page+1)%pages();update();resetResearch()};addEventListener('resize',()=>{const old=perView;perView=calc();if(old!==perView){page=0;renderDots()}update()});perView=calc();renderDots();update();let researchTimer;const resetResearch=()=>{clearInterval(researchTimer);researchTimer=setInterval(()=>{page=(page+1)%pages();update()},4800)};resetResearch();
const metricItems=[['Publications',metrics.publications],['Citations',metrics.citations],['h-index',metrics.h_index],['Open Access',metrics.open_access_works]];document.getElementById('metrics').innerHTML=metricItems.map(([l,v])=>`<div class="metric"><div class="metric-value" data-count="${Number(v||0)}">0</div><div class="metric-label">${l}</div></div>`).join('');document.getElementById('metric-status').textContent=metrics.last_updated?`OpenAlex updated ${new Date(metrics.last_updated).toLocaleDateString()}`:metrics.status;if('IntersectionObserver'in window){const cio=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting)return;const el=e.target,target=Number(el.dataset.count||0),start=performance.now(),duration=1100;const step=now=>{const q=Math.min(1,(now-start)/duration);el.textContent=BERL.fmt(Math.round(target*(1-Math.pow(1-q,3))));if(q<1)requestAnimationFrame(step)};requestAnimationFrame(step);cio.unobserve(el)}),{threshold:.35});document.querySelectorAll('[data-count]').forEach(el=>cio.observe(el))}else document.querySelectorAll('[data-count]').forEach(el=>el.textContent=BERL.fmt(el.dataset.count));
const recent=[...pubs].sort((a,b)=>(b.year||0)-(a.year||0)).slice(0,4);document.getElementById('home-pubs').innerHTML=recent.length?recent.map(p=>`<article class="publication"><div class="pub-year">${p.year||''}</div><div><div class="pub-title">${BERL.esc(p.title)}</div><div class="pub-meta">${BERL.esc((p.authors||[]).join(', '))}${p.journal?' · '+BERL.esc(p.journal):''}</div></div><div class="pub-links">${p.doi?`<a href="https://doi.org/${BERL.esc(p.doi)}" target="_blank">DOI ↗</a>`:''}</div></article>`).join(''):'<div class="empty">Publications are populated automatically from OpenAlex.</div>';document.getElementById('home-news').innerHTML=latest.slice(0,3).map((n,i)=>`<a class="card news-card" href="${detail(n)}"><div class="news-image sprite-photo" style="background-position:${BERL.spritePos(n.cover_photo_index??n.photo_index??i)}"></div><div class="news-copy"><div class="date">${BERL.esc(n.date)} · ${BERL.esc(n.category)}</div><h3>${BERL.esc(n.title)}</h3><p>${BERL.esc(n.summary)}</p><span class="read-more">Read full story →</span></div></a>`).join('');const scene=document.getElementById('motion-scene'),objects=[...document.querySelectorAll('.motion-object')];const animateScene=()=>{if(!scene)return;const r=scene.getBoundingClientRect(),p=Math.max(0,Math.min(1,(innerHeight-r.top)/(innerHeight+r.height)));objects.forEach((el,i)=>{const dir=i%2?1:-1;el.style.transform=`translate3d(${dir*(1-p)*70}px,${(1-p)*(44+i*12)}px,0) rotate(${dir*(1-p)*4}deg)`});scene.style.setProperty('--scene-progress',p)};addEventListener('scroll',animateScene,{passive:true});animateScene()}catch(e){console.error(e)}});