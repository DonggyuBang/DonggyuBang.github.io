document.addEventListener("DOMContentLoaded",async()=>{
  const [pubs,metrics]=await Promise.all([BERL.json("data/publications.json"),BERL.json("data/metrics.json")]);
  const q=document.getElementById("pub-search"),year=document.getElementById("pub-year"),type=document.getElementById("pub-type");
  [...new Set(pubs.map(p=>p.year).filter(Boolean))].sort((a,b)=>b-a).forEach(y=>year.insertAdjacentHTML("beforeend",`<option>${y}</option>`));
  [...new Set(pubs.map(p=>p.type).filter(Boolean))].sort().forEach(t=>type.insertAdjacentHTML("beforeend",`<option value="${BERL.esc(t)}">${BERL.esc(t)}</option>`));
  document.getElementById("pub-status").textContent=metrics.last_updated?`OpenAlex sync: ${new Date(metrics.last_updated).toLocaleString()} · ${metrics.publications} works · ${BERL.fmt(metrics.citations)} citations`:`Not synchronized yet. Run the GitHub Actions workflow after upload.`;
  function render(){const s=q.value.trim().toLowerCase();const list=[...pubs].sort((a,b)=>(b.year||0)-(a.year||0)).filter(p=>{const hay=[p.title,p.journal,(p.authors||[]).join(" "),p.doi,(p.topics||[]).join(" ")].join(" ").toLowerCase();return(!s||hay.includes(s))&&(!year.value||String(p.year)===year.value)&&(!type.value||p.type===type.value)});document.getElementById("pub-count").textContent=`${list.length} publication${list.length===1?"":"s"}`;document.getElementById("pub-list").innerHTML=list.length?list.map(p=>`<article class="publication"><div class="pub-year">${p.year||""}</div><div><div class="pub-title">${BERL.esc(p.title)}</div><div class="pub-meta">${BERL.esc((p.authors||[]).join(", "))}${p.journal?" · "+BERL.esc(p.journal):""}${p.cited_by_count?` · ${BERL.fmt(p.cited_by_count)} citations`:""}</div></div><div class="pub-links">${p.doi?`<a href="https://doi.org/${BERL.esc(p.doi)}" target="_blank" rel="noopener">DOI ↗</a>`:""}${p.url?`<a href="${BERL.esc(p.url)}" target="_blank" rel="noopener">Source ↗</a>`:""}</div></article>`).join(""):`<div class="empty">${pubs.length?"No publications match your filters.":"No synchronized publications yet. Run the GitHub Actions workflow."}</div>`}
  q.addEventListener("input",render);year.addEventListener("change",render);type.addEventListener("change",render);render();
});
